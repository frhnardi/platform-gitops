apiVersion: v1
kind: Service
metadata:
  name: weather-api
  labels:
    app.kubernetes.io/name: weather-api
spec:
  selector:
    app.kubernetes.io/name: weather-api
  ports:
    - name: http
      port: 8080
      targetPort: http
