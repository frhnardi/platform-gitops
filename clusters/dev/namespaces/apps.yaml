apiVersion: v1
kind: Namespace
metadata:
  name: apps
  labels:
    # App tier: baseline + supply-chain policies run in Enforce here. The 3.2
    # baseline policies target this label via namespaceSelector.
    paved-road.platform/tier: app
    app.kubernetes.io/part-of: platform-gitops
